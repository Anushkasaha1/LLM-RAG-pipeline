# LLM-RAG-pipeline
A simple Retrieval-Augmented Generation (RAG) pipeline that loads documents, splits them into chunks, creates embeddings, stores them in a vector database, and answers user queries using retrieved context.
